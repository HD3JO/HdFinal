<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hyundai.project.user.repository.TestUserMapper">
    <select id="getUser" parameterType="com.hyundai.project.dto.MemberDTO" resultType="HashMap">
        SELECT * FROM TABLE(FN_GET_MEMBER_INFO(#{email, jdbcType=VARCHAR}, #{password, jdbcType=VARCHAR}, #{name, jdbcType=VARCHAR}, 
        #{address, jdbcType=VARCHAR}, #{phone, jdbcType=VARCHAR}, #{channel, jdbcType=VARCHAR}, #{enable, jdbcType=NUMERIC}, 
        #{role, jdbcType=VARCHAR}))
    </select>
</mapper>